node_modules/
dist/
out/
release/
*.log
.DS_Store
Thumbs.db
.env
.env.local
*.tsbuildinfo

# 本地运行时数据（会话日志、工作区缓存），不入库
.deepwork/

# 助手侧的本机记忆/工作日志（.workbuddy/），属于本地工作数据，不入库
.workbuddy/

# 调试时把各套测试输出重定向到根目录留下的残留（.out1.txt / .skill-out.txt 之类）
# ——它们只是 verify.log 的重复片段，不该混进提交。
.out*.txt
.*-out.txt

# 调试产物（演示工作区、本地运行数据、临时截图）——全部不入库
artifacts/
# 例外：各里程碑的验收截图属于交付证据，由 git add -f 显式纳入（见 docs/DEVLOG.md 的「验证」段）。
# 它们可由 DEEPWORK_CAPTURE 环境变量随时重新生成，因此不随代码变更自动跟踪。

# 应用图标：由 tools/make-icon.js 从主题色常量生成，属于可复生产物。
# 改主题色后重新打包即可同步，不需要手工维护一份二进制。
apps/desktop/build/icon.png

# 一体化安装包的随包运行时暂存区（便携 node.exe、dsh 依赖），体积数百 MB 且可重建，
# 重建步骤见 offline-bundle/使用说明.txt 与 docs/DEVLOG.md
offline-bundle/
